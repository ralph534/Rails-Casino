<div class="pot">
  Pot: <span id="pot"></span>
</div>

<div class="poker-table">
  <div class="flop">
  </div>
  <div class="poker-hand">
    <% @cards.each do |c| %>
      <%= image_tag c.avatar_file_name %>
    <% end %>
  </div>
</div>

<%= render 'footer' %>